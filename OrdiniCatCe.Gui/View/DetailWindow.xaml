<Window x:Class="OrdiniCatCe.Gui.View.DetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        DataContext="{Binding DetailViewModel, Source={StaticResource Locator}}"
        Title="DetailWindow" Height="300" Width="600">
    <StackPanel>
        <StackPanel.Resources>
           
            <Style TargetType="Border">
                <Setter Property="CornerRadius" Value="5"></Setter>
                <Setter Property="Margin" Value="4"></Setter>
            </Style>
            <Style TargetType="Button">
                <Setter Property="Margin" Value="4"></Setter>
            </Style>
        </StackPanel.Resources>
        <Border x:Name="brdCliente" BorderThickness="1" BorderBrush="Chartreuse" >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="2*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="2*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>

                <TextBlock Text="Cliente"></TextBlock>
                <TextBox Text="{Binding Cliente, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" />
                
                <TextBlock Text="Indirizzo" Grid.Column="2"></TextBlock>
                <TextBox Text="{Binding Indirizzo, UpdateSourceTrigger=PropertyChanged}" Grid.Column="3" />
                
                <TextBlock Text="Telefono" Grid.Row="1"></TextBlock>
                <TextBox Text="{Binding Telefono, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1" />
                
                <TextBlock Text="EMail" Grid.Row="1" Grid.Column="2"></TextBlock>
                <TextBox Text="{Binding EMail, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="3" />

                <TextBlock Text="Cellulare" Grid.Row="2" ></TextBlock>
                <TextBox Text="{Binding Cellulare, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="1" />
            </Grid>
        </Border>
        <Border x:Name="brdPezzo"></Border>
        <Border x:Name="brdOrdine"></Border>

        <Border x:Name="btnBorder">
            <StackPanel Orientation="Horizontal">
                <Button Content="Annulla" Command="{Binding AnnullaCommand}"/>
                <Button Content="Conferma" Command="{Binding ConfermaCommand}"/>
            </StackPanel>
        </Border>
    </StackPanel>
</Window>
